{
  "name": "controllertest",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "mutate": "stryker run",
    "cypress:run": "npx cypress run --spec cypress/e2e/juice-shop.cy.js --browser electron",
    "test:all": "npm run test:coverage && npm run mutate && npm run cypress:run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.0",
    "@stryker-mutator/jest-runner": "^9.6.0",
    "cypress": "^14.5.4",
    "jest": "^29.7.0"
  }
}
